[project]
name = "ner-field-guide"
version = "0.1.0"
description = "Companion code for The Definitive Guide to NER in 2026"
requires-python = ">=3.11"
dependencies = [
    "gliner>=0.2.0",
    "transformers>=4.40.0",
    "torch>=2.0.0",
    "onnxruntime>=1.17.0",
    "instructor>=0.4.0",
    "openai>=1.12.0",
]
